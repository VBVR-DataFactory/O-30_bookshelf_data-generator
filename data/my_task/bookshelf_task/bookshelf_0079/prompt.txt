Given a row of blue books (existing books on shelf) and red books (to insert), determine where each red book should be inserted. Blue books form clusters based on adjacent height differences. Each cluster has a representative height (mean). Assign each red book to the cluster with the nearest representative height, and insert it at the end of that cluster. Output the 0-based insertion index for each red book.